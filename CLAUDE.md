# AI_TUTOR_PROJECT

## SDD 豁免声明

**本项目豁免 SDD 流程。**

- 所有文件改动**不走** Backlog / Skill / Phase 链路，按用户自然语言指令直接执行
- routing.md 中 Read-Only / SDD / Team 三模式判断降级为：**Read-Only（只读问答）** 或 **Direct-Edit（直接改文件）** 两类
- `/sdd:*` skills 仅在用户**显式调用**时启用（如 `/sdd:setup`、`/sdd:define-feature` 等）
- SessionStart 提示"项目未初始化"可忽略
- 如需重启 SDD，删除本文件的"豁免声明"小节即可

---

## 项目说明

> 本节按需增补。目前作为 SDD 豁免声明的宿主文件。

- 产品规格：`00-public/1-product/spec/design/`（v0.4.0 已审阅完成）
- 功能树：`00-public/1-product/feature-tree/`（L1+L2 全量，manifest.md 为索引）
- 决策记录：`00-public/4-decisions/`
