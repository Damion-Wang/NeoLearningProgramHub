# 功能树 05：Report（报告场域）

**Owner：** 娄跃、陈波
**日期：** 2026-04-22
**版本：** v0.4.0 对齐版（L1+L2）
**对应 Spec：** `spec/design/learner/05-report-classroom.md`

---

## 一句话定位

学员查看个人学习报告 + 与 Neo 讨论解读。报告不是"宣判"，是"对话"，可质疑、提问、共同解读成长。

---

## 功能结构

```
A. Neo 在报告场域
   A1 Neo 身份（Tutor，可访问统一 Database 全部数据，关注聚焦当前 Course 维度）
   A2 报告解读引导（渐进式呈现）
   A3 证据追溯（逐层钻取：结论→证据摘要→原始对话）
   A4 知识关联（报告讨论中关联课程知识点）
   A5 ★报告访问权限矩阵（学员/管理员三重入口统一）[补漏]
       - 学员自查：仅自己 · 不见排名和分组标签 · 只见分位区间
       - 管理员代查：全员报告 + 排名 + 分组标签（见 06.D1）
       - 管理员体验：通过 Topbar 端口切换以学员身份看，按学员权限呈现
       - API 级隔离：学员端 API 不返回排名/分组标签字段（见 F4）

B. 报告三部分结构 ★v0.4.0 简化（从 8 模块 → 3 部分）
   B1 第一部分：学了什么（内容回顾 — 关键知识点和方法论）
   B2 第二部分：怎么表现的（表现复盘 — 高光 + 待改进，具体案例支撑）
   B3 第三部分：接下来做什么（应用计划 — 可执行步骤）

C. 报告生成
   C1 触发与入口（Course 结束后固定生成，本期不做辅导随时进入）
   C2 渐进式加载（祝贺动画 + 后台并行生成 + 分段呈现）
   C3 碎片汇聚降级（超时 >30s / 失败三级降级：已得渲染 / 标"数据收集中" / 重试 3 次失败标"数据不足"）
   C4 异常恢复与断点恢复（重新进入已生成报告 · 生成中断已计算模块缓存复用 · Neo 对话历史保留可接续讨论，沿用 07.M）
   C5 情感化文案（成长型语言 + 个性化引用具体行为 + 数据不足兜底）

D. BI 图表（通用组件库，跨端复用）
   D1 五种图表（雷达 / 热力 / 饼 / 折线 / 柱状）
   D2 图表 ↔ 部分映射（B1 纯文字 / B2 雷达 + 热力 + 柱 / B3 纯文字）
   D3 可交互（点击图表数据点 → Neo 解读）
   D4 数据可追溯（点击数据点查看原始记录）
   D5 联动事件协议 `[REF:chart=xx,dim=yy]`（组件库跨端复用，也供 Operation 消费）

E. 报告解析（双向讨论）
   E1 提问报告内容（Neo 引用行为证据回答）
   E2 质疑评估（不是宣判是对话，双向讨论）
   E3 深入解读（按需展开）
   E4 讨论不修改报告（反馈保存供模型优化）

F. 成长时间线 + 匿名分位
   F1 个人成长时间线（跨 Course 折线，维度 ID 匹配对齐非名称匹配）
   F2 单/双 Course 降级（单点用雷达快照 / 双 Course 加"趋势仅供参考"提示）
   F3 匿名分位区间（柔和灰色标尺，前 25% / 中 50% / 后 25%，不展示名次）
   F4 ★API 级隔离（学员端不返回排名字段，防前端泄漏）
   F5 成长幅度计算（消费 Evaluation 的结果）

G. 导出 PDF
   G1 学员导出（含图表静态截图 + 文字解读）
   G2 图表服务端静态化（渲染为图片 + AI 生成文字解读）
   G3 批量导出接口（本期不做，预留给管理端）

H. 报告数据消费（消费 Evaluation 产出）
   H1 学习特质碎片（Activity 级）
   H2 认知转化碎片（Quiz 级）
   H3 能力评估碎片（对练级）
   H4 被动信号碎片（会话级）
   H5 BEI 案例碎片（调研）
   H6 行为改变证据链汇聚（初始 → 过程 → 最终）

I. 布局（两栏 + 可折叠导航）
   I1 可折叠导航区（Project 目录）
   I2 左栏：报告内容（三部分 + BI 图表，渐进式呈现）
   I3 右栏：Neo 对话（质疑 / 追问 / 解读）
   （笔记悬浮球常驻，见 01.F）
```

---

## 跨模块依赖

| 对象模块 | 依赖关系（本模块节点 ← 对方节点） |
|---------|--------------------------------|
| Evaluation (09-A) | H 所有碎片 ← `09-A.B1-B5` · F5 成长幅度 ← `09-A.D` · F3 分位 ← `09-A.G` |
| Infra (07) | D 图表组件库 ← `07.L` 联动协议 · I 布局两栏 ← `07.K7` · G2 PDF 服务端渲染 ← `07` · C4 断点 ← `07.M` |
| Hall (01) | C1 报告入口 ← `01.G1/G3` Leo 推荐或自动衔接 |
| Operation (06) | D 图表组件库跨端复用 → `06.D` · G3 批量导出接口预留 → `06.D6.4` |
| Lecture / Practice / Inquiry | 信号采集上游 → `02.I` / `03.G` / `04.F` |

---

## 术语速查

- **维度 ID 匹配** = 跨 Course 比较时使用维度的稳定 ID（而非显示名称），避免维度改名导致时间线断裂
- **碎片** = Evaluation 产出的结构化评估单元（主定义见 `09-A.B`）
  - 学习特质（Activity 级）/ 认知转化（Quiz 级）/ 能力评估（对练级）/ 被动信号（会话级）/ BEI 案例（调研级）
- **分位区间** = 前 25% / 中 50% / 后 25% 三档，学员端仅见分位不见具体名次
- **成长幅度** = 学员跨时间进步的量化指标（BEI 基线优先，无基线用前后段均值 + z-score）

---

## 待确认

| # | 问题 | 建议 |
|---|------|------|
| 1 | 三部分结构下，原 0.3.3 B3-B8 六模块的数据是否还采集？ | 建议保留采集（碎片），只在呈现时折叠进"第二部分表现复盘" |
| 2 | 单 Course 降级用雷达快照 vs 纯文字？ | 建议雷达快照，保留视觉锚点 |
| 3 | BEI 基线缺失时成长幅度展示精度（显著 / 中等 / 轻微）粒度够吗？ | 建议够，避免伪精确数字引发争议 |
| 4 | D5 联动事件协议是否抽到 Infra 作为通用能力？ | 建议抽到 Infra L 节（看板↔对话联动） |
| 5 | Neo 在报告场域的"知识关联"如何不越权（不开始教学）？ | 需定义边界：仅提示方向 / 关联课程入口 / 不做深度讲解 |
